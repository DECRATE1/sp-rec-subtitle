sp-rec-subtitle
Проект для автоматической генерации субтитров из аудио/видео файлов. Он объединяет инструменты распознавания речи и обработки мультимедиа, чтобы создавать SRT/ASS субтитры и встраивать их в видео.

📌 Возможности
Извлечение аудио из видео (ffmpeg).

Распознавание речи и генерация текста субтитров.

Поддержка форматов SRT и ASS.

Автоматическое наложение субтитров на видео.

Гибкая настройка стиля субтитров (цвет, фон, шрифт).

📂 Структура проекта
main.py — основной скрипт для запуска.

requirements.txt — список зависимостей.

video_test.mp4 — тестовый видеоролик.

extracted_audio.mp3 — пример извлечённого аудио.

subtitles.srt / subtitles.ass — примеры сгенерированных субтитров.

output.mp4 — видео с встроенными субтитрами.

⚙️ Установка
bash
git clone https://github.com/DECRATE1/sp-rec-subtitle.git
cd sp-rec-subtitle
pip install -r requirements.txt
🚀 Использование
Поместите видеофайл в папку проекта.

Запустите основной скрипт:

bash
python main.py
На выходе вы получите:

subtitles.srt и/или subtitles.ass

output.mp4 — видео с встроенными субтитрами

🛠️ Зависимости
Python 3.9+

ffmpeg — для работы с аудио/видео

Библиотеки из requirements.txt (например, moviepy, whisper, pillow и др.)

📖 Пример работы
Вход: video_test.mp4

Выход: output.mp4 с автоматически сгенерированными субтитрами.